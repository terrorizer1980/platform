import { NgModule } from "@angular/core";
import { CoinsRoutingModule } from "./coins-routing.module";
import { RouterModule } from "@angular/router";

@NgModule({
  declarations: [],
  imports: [CoinsRoutingModule, RouterModule],
  providers: []
})
export class CoinsModule {}
